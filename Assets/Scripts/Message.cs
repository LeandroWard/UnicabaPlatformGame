using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//Escribiendo mensaje de prueba

public class Message : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
